import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.photoboother.christmaslighttakedownoptimizer',
  appName: 'christmas-light-takedown-optimizer',
  webDir: 'out'
};

export default config;
